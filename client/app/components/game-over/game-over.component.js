import template from './game-over.html';
import controller from './game-over.controller';

let gameOverComponent = {
    restrict: 'E',
    bindings: {},
    template,
    controller
};

export default gameOverComponent;
